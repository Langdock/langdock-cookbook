{
  "name": "private-network-mcp-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "IP-allowlisted public hop in front of a private Streamable HTTP MCP",
  "scripts": {
    "d": "pnpm i && pnpm dev",
    "build": "tsc",
    "dev": "tsc && node dist/index.js",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "typescript": "^5.7.0"
  }
}
